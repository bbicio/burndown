# Environment
.env

# Node
api/node_modules/
node_modules/

# OS
.DS_Store
Thumbs.db

# RTK / local tooling
.tokensave/
.claude/settings.local.json.bak

# Worktrees
.worktrees/

# scripts/test-branch.sh generates this override file per branch; never source-controlled
docker-compose.branch.yml

# scripts/run-tests.sh generates this override file per run; never source-controlled
docker-compose.test.yml
.run-tests-image-hash
