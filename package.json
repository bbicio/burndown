{
  "name": "pdash-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "PDash frontend test toolchain (dev-only; never bundled or served)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vitest": "^4.1.9"
  }
}
